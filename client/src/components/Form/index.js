const formstyle = theme => ({
  root: {
    display: "flex",
    justifyContent: "center",
    marginTop: "5rem",
    marginBottom: "5rem"
  },
  card: {
    minWidth: 300,
    maxWidth: 600,
    paddingLeft: "2rem",
    paddingRight: "2rem"
  },
  cardAction: {
    display: "inherit"
  }
});

export default formstyle;
